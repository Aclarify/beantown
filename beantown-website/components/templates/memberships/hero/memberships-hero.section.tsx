import React from 'react';
import MembershipsHeroContent from './memberships-hero.content';

const MembershipsHeroSection = () => {
	return (
		<section id="memberships-hero" className="z-5 relative">
			<MembershipsHeroContent />
		</section>
	);
};

export default MembershipsHeroSection;

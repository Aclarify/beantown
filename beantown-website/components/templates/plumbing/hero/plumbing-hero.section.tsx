import React from 'react';
import PlumbingHeroContent from './plumbing-hero.content';

const PlumbingHeroSection = () => {
	return (
		<section id="plumbing-hero" className="z-5 relative">
			<PlumbingHeroContent />
		</section>
	);
};

export default PlumbingHeroSection;
